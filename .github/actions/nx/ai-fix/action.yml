name: NX for AI fix
description: Nx Cloud fixes - https://nx.dev/ci/features/self-healing-ci

runs:
  using: composite
  steps:
    - name: cloud-cli | nx/AI fix
      if: always()
      shell: bash
      run: |
        npx nx fix-ci
    