name: whats_new-mock | deployer

on:
  workflow_call:
    inputs:
      CALLER:
        required: true
        type: string

jobs:
  tva:
    name: dc/TVA
    if: ${{ inputs.CALLER == 'timeline' }}
    uses: vezham/vx-fe-deployer/.github/workflows/whats_new-mock.helper.yml@timeline
    secrets:
      VERCEL_SA_KEY: ${{ secrets.WHATS_NEW_MOCK_TVA_VERCEL_SA_KEY }}
    with:
      GIT_BRANCH: ${{ inputs.CALLER }}
      IS_DEBUG: true
      IS_BETA: true
      APP_NAME: whats_new-mock
